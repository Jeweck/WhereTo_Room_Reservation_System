rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype Mode: Full open access for development.
    // Manual sync check: Ensure this matches the Firebase Console.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}