
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Wide open rules for unblocked prototype development.
    // This allows all read and write operations from any source.
    // Forced redeploy triggered for bookings creation.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
