
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open prototype rules for unblocked development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
