rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype Mode: Full open access for development.
    // Sync Version: 1.0.9 (Forced Redeploy)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}